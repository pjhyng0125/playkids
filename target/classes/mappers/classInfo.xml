<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="classInfo">
	<select id="select_info" resultType="ClassVO">
		select cname, cpic, cage, cintro, cdate, cteachername, cteacher, cteacherpic, prepare, notice
		from class 
		where cname=#{cname}
	</select>
	
	<select id="select_business" resultType="BusinessVO">
		select bid,bname, bintro,bphone,baddress
		from business
		where bid=#{bid}
	</select>
</mapper>